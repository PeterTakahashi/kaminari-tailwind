# frozen_string_literal: true

RSpec.describe Kaminari::Tailwind do
  it "has a version number" do
    expect(Kaminari::Tailwind::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
